{
  "version": 2,
  "ignoreCommand": "echo IGNORING ROOT",
  "builds": [
    {
      "src": "react-site/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "react-site/dist/$1" }
  ]
}

