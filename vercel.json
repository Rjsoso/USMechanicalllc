{
  "buildCommand": "cd react-site && npm install && npm run build",
  "outputDirectory": "react-site/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ],
  "github": {
    "silent": false
  }
}
